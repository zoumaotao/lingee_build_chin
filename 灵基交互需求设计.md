# 灵基交互需求设计

> 适用范围：灵基 Work 平台上的 HelpDesk 交互能力  
> 整理日期：2026-08-13  
> 依赖文档：振兴 Helpdesk 需求清单（开发版 0813）、HelpDesk 月底上线冲刺计划  
> 状态：设计中

---

## 一、系统角色定义

| 角色 | 说明 | 可执行操作 |
|------|------|-----------|
| Requester（提交者） | 普通员工 | 创建工单、查看本人工单、多次回复、确认解决/驳回、评价 |
| Agent（处理专员） | IT/HR 处理人 | 查看分配给自己的工单、多次回复、提交已解决、部门内转派、修改工单类别（触发流转） |
| Manager（部门经理） | 业务处理组管理者 | 查看所属部门工单、分配处理人、跨组转派、触发自动分配、查看部门看板 |
| Admin（系统管理员） | 超级管理员 | 全局操作、分类/SLA/节假日配置、组织和用户导入 |

**关键约束**：
- Manager **不处理工单**，只负责分配和转派
- Agent 不能跨组直接转派，需修改类别后提交本组 Manager
- 工单最终关闭必须由提交人确认

---

## 二、工单状态定义

| 状态 | 说明 | 进入条件 | 可执行操作 |
|------|------|---------|-----------|
| 新建 | 工单刚创建，尚未完成路由 | 用户或 AI 创建工单 | AI 识别分类/优先级、规则匹配 |
| 待分配 | 已识别处理组但未指定处理人 | 路由到业务处理组但未匹配到具体人、Agent 修改分类后 | Manager 指定人员、转交其他组、触发自动分配 |
| 处理中 | 已指定具体处理人 | 自动分配、Manager 分配、转派、提交人驳回 | 多次回复、转交、提交已解决 |
| 待提交人确认 | 处理人认为已解决 | 处理人执行"提交已解决" | 提交人确认解决或驳回 |
| 已关闭 | 提交人确认问题已解决 | 提交人执行"确认解决" | 查看、评价 |

**状态流转规则**：
- **回复不改变状态**：Agent 答复后工单仍为"处理中"
- **转派不改变状态**：转给新处理人后仍为"处理中"
- **驳回回到处理中**：提交人认为未解决，工单回到"处理中"
- **关闭必须提交人确认**：Agent 只能"提交已解决"，不能直接关闭

```
新建 → 待分配 → 处理中 ⇄ 待提交人确认 → 已关闭
         ↑                    ↓ (驳回)
         |____________________↑
         
处理中 → Agent修改分类 → 待分配(流转本组Manager)
```

---

## 三、灵基 Work 应用菜单

### 3.1 菜单结构

Work 左侧菜单下挂载工单后台应用页面：

| 菜单 | 面向角色 | 功能 |
|------|---------|------|
| 我的工单 | Requester | 查看本人提交的工单 |
| 我的任务 | Agent | 查看分配给自己的工单 |
| 运营仪表盘 | Manager / Admin | 部门看板、SLA 分析、分类统计 |

### 3.2 我的工单（Requester 视角）

**视图标签**：全部 / 处理中 / 待评价 / 已关闭

| 视图 | 包含状态 |
|------|---------|
| 全部 | 本人提交的全部工单 |
| 处理中 | 新建 + 待分配 + 处理中 |
| 待评价 | 待提交人确认（处理人已提交已解决） |
| 已关闭 | 已关闭 |

**页面能力**：
- 工单列表（工单号、标题、状态、优先级、SLA、处理人、最后更新时间）
- 点击进入详情页（进度时间线、回复记录、附件）
- 详情页操作：多次回复、确认解决、驳回（仅"待提交人确认"状态）
- 新建工单入口
- 统计摘要

### 3.3 我的任务（Agent 视角）

**视图标签**：全部 / 处理中 / 待评价 / 已关闭

| 视图 | 包含状态 |
|------|---------|
| 全部 | 分配给本人的全部工单 |
| 处理中 | 处理中（本人为当前处理人） |
| 待评价 | 待提交人确认（本人已提交已解决） |
| 已关闭 | 已关闭 |

**页面能力**：
- 工单列表（工单号、标题、提单人、状态、优先级、SLA 剩余/超时、最后更新）
- SLA 超时工单红色高亮，按紧迫度排序
- 点击进入详情页
- 详情页操作：
  - 多次回复（回复不改变状态）
  - 提交已解决（独立按钮，改变状态为"待提交人确认"）
  - 部门内转派（选目标人+填原因）
  - 修改工单分类（保存后清除当前处理人，流转至本组 Manager）
- 统计摘要（待处理数、SLA 即将超时、今日已处理、SLA 达标率）
- 新工单分配后自动出现在列表顶部

### 3.4 运营仪表盘（Manager 视角）

**页面能力**：
- 按业务处理组、处理人、公司、一级分类、二级分类筛选
- 指标：工单量、未关闭量、状态分布、SLA 达成率、平均响应时间、平均解决时间
- 待分配队列：Manager 可从此处分配处理人或触发自动分配
- 团队成员负载一览

### 3.5 通知方式

- 第一期只使用邮件通知
- 覆盖：创建、分配、转派、回复、待确认、驳回、关闭、SLA 预警、休假自动回复
- Work 页面通过实时查询工单后台展示最新状态，不依赖灵基平台级推送

### 3.6 一键发起会话

- 入口纳入 8/14 Work 菜单目标
- 是否携带 ticketId/描述/附件上下文待 8/13 对齐
- 未确认前只提供"打开智能体"通用新会话
- 明确提示"不携带当前工单上下文"
- 不得把敏感工单内容直接拼入 URL

---

## 四、灵基交互 — 员工流程（Requester）

### 4.1 卡片清单

员工对话流程共 **4 张卡片**：

| # | 卡片 | 触发时机 | 组件 |
|---|------|---------|------|
| 1 | 知识检索卡 | 员工提问后，智能体从知识库找到可能的解决方案 | employee/resolution |
| 2 | 工单草稿卡（含附件） | 知识没解决，AI 根据对话生成草稿 | employee/ticket-draft |
| 3 | 工单列表卡 | 员工查看自己的工单列表 | employee/ticket-list |
| 4 | 工单详情卡 | 一切和工单相关的展示（数据驱动） | employee/ticket-detail |

### 4.2 完整流程

```
员工提问
  ↓
① 知识检索卡 → 已解决？→ 结束
                → 仍未解决 ↓
② 工单草稿卡（AI 自动填充分类/优先级，含附件勾选）→ 确认提交
  ↓
④ 工单详情卡（后台返回 status=created，显示工单号+团队+待分配）
  ↓
  ... 工单进入路由/分配/处理 ...
  ↓
  员工查进度 → ③ 工单列表卡 → 点某张 → ④ 工单详情卡
  或直接说"看下 VPN 那个工单" → ④ 工单详情卡
  ↓
  有答复时显示"已解决 / 未解决驳回"按钮
  ↓
  已解决 → 卡片内切换为评分 → 提交评价 → 闭环
  驳回 → 卡片内切换为原因输入 → 提交驳回 → 工单回到处理中
```

### 4.3 各卡片设计

#### ① 知识检索卡（employee/resolution）

- 展示：知识来源、方案标题、解决步骤、提问"解决了吗？"
- 按钮："已解决" / "仍未解决，创建工单"
- 无命中时如实说明，不编造

#### ② 工单草稿卡（employee/ticket-draft）

- 展示：提单人（自动，不可改）、标题、分类（AI 填充可编辑）、优先级（AI 判断可编辑）、描述、附件（勾选框）
- 按钮："保存草稿" / "确认提交"
- **AI 识别**：AI 根据问题描述自动判断一级分类（23 个）和优先级。**智能体提单只处理到一级分类**，二级分类由 Agent 后续补充
- 附件规则：PDF/JPG/PNG/TXT/LOG，单个 ≤10MB，最多 5 个，全部成功才建单

#### ③ 工单列表卡（employee/ticket-list）

- 展示：最近更新的工单（3-5 条），每条有工单号、标题、状态
- 点击某条 → 展示详情卡
- "查看更多" → 跳转 Work "我的工单"页面

#### ④ 工单详情卡（employee/ticket-detail）— 数据驱动

**核心原则**：智能体只需调一个 Tool（Get_Ticket_Detail 或 Create_Ticket 的返回值），后台返回什么字段就展示什么。不区分"回执卡""进度卡""确认卡"——全部由同一张详情卡根据数据自适应。

**展示规则（有什么数据就显示什么）**：

| 数据字段 | 有值时展示 | 无值时 |
|---------|-----------|--------|
| ticketId | 工单号 | 不显示 |
| title | 工单标题 | 不显示 |
| status | 状态 badge | 不显示 |
| description | 问题描述 | 不显示 |
| assignee / team | 处理人 + 团队 | 显示"待分配" |
| sla | SLA 时限 | 不显示 |
| latestReply + author + time | 答复区域 | 不显示 |
| timeline | 处理轨迹（有答复时折叠，无答复时展开） | 不显示 |

**按钮规则（由状态决定）**：

| 条件 | 按钮 |
|------|------|
| status = "created"，无答复 | 查看详情（跳转 Work 页面） |
| 处理中，无答复 | 查看详情 |
| 有 latestReply（待提交人确认） | **已解决** / **未解决驳回** |
| 点"已解决"后（step=rate） | 评分星星 + 标签 → 跳过评价 / 提交评价 |
| 点"驳回"后（step=reject） | 原因输入框 → 取消 / 提交驳回 |

**自然语言直接操作**：

- "已解决了，我来评价" → 智能体先调 Close_Ticket → 传 `{ step: "rate" }` 直接进评分
- "没解决" → 传 `{ step: "reject" }` 直接进驳回原因输入
- "看下这张工单" → 调 Get_Ticket_Detail → 传返回数据展示详情

## 五、灵基交互 — 处理人流程（Agent）

### 5.1 卡片清单

处理人对话流程共 **5 张卡片**：

| # | 卡片 | 触发时机 | 组件 |
|---|------|---------|------|
| 1 | 处理人工单列表卡 | 查看待处理工单 | agent/queue |
| 2 | 处理人工单详情卡 | 查看某张工单 | agent/ticket-detail |
| 3 | 答复确认卡 | 草拟答复后确认发送 | agent/reply |
| 4 | 转派确认卡 | 部门内转交工单 | agent/reassign |
| 5 | 知识产物卡 | 处理完成后沉淀知识 | knowledge/knowledge-draft |

### 5.2 完整流程

```
处理人打开会话
  ↓
"看看我的待办" → ① 处理人工单列表卡
  ↓
点击某张 → ② 处理人工单详情卡
  ↓
  ├── "帮我草拟答复" → ③ 答复确认卡 → 确认发送（状态不变，仍为处理中）
  ├── "提交已解决" → 状态变为"待提交人确认"
  ├── "转给 XX" → ④ 转派确认卡（部门内）→ 确认转派（状态不变）
  ├── "改分类" → 分类修改 → 清除处理人 → 流转本组Manager（变为待分配）
  └── "生成知识" → ⑤ 知识产物卡 → 确认沉淀
```

### 5.3 各卡片设计

#### ① 处理人工单列表卡

- 展示：待处理工单列表（按 SLA 紧迫度排序），工单号、标题、提单人、SLA、优先级
- 超时工单高亮
- 点击 → 详情卡

#### ② 处理人工单详情卡

- 展示：标题、工单号、状态、提单人、问题描述、SLA、附件、处理时间线
- 按钮：
  - "答复" → 触发答复卡
  - "提交已解决" → **独立按钮**，改变状态为"待提交人确认"
  - "转派" → 触发转派卡（部门内）
  - "修改分类" → 弹出分类选择，保存后清除处理人，流转 Manager

**关键**：
- "答复"和"提交已解决"是两个独立操作
- 答复只发送回复，不改变状态
- 提交已解决才改状态
- Agent 不能直接关闭工单

#### ③ 答复确认卡

- 展示：工单号、回复对象、答复内容（AI 草拟可编辑）、内部备注（选填）
- 按钮："确认并发送" / "修改"
- 发送成功后**状态仍为处理中**

#### ④ 转派确认卡

- 展示：工单号、目标处理人（下拉，限本组）、转派原因（必填）
- 按钮："确认转派" / "取消"
- 规则：
  - Agent 只能部门内转派
  - 跨组需先修改分类 → 流转本组 Manager → Manager 决定
  - 转派后状态保持"处理中"，只更换处理人

#### ⑤ 知识产物卡

- 展示：来源工单、知识标题（AI 生成可编辑）、处理方法/步骤、标签
- 按钮："确认沉淀" / "编辑" / "跳过"
- 确认后进入待审核，不直接发布
- DC 写入为 8/21 计划能力

---

## 六、Manager 交互

Manager 不通过智能体对话处理工单，而是通过 Work 菜单页面操作：

### 6.1 Manager 页面能力

| 功能 | 说明 |
|------|------|
| 待分配队列 | 查看本组"待分配"工单，指定处理人 |
| 触发自动分配 | 对待分配工单执行自动分配（按负载均衡） |
| 跨组转交 | 将工单转交给其他组指定人员或其他分组 |
| 部门看板 | 按处理人/分类/公司查看工单量、SLA、满意度 |
| 休假管理提醒 | 处理人进入休假时收到未完成工单提醒 |

### 6.2 Manager 不做的事

- 不回复工单
- 不提交已解决
- 不关闭工单
- 不操作系统级配置（那是 Admin）

---

## 七、Skill 能力与 Tool 映射

### 7.1 员工 Skill

| 能力 | MCP Tool | 卡片 |
|------|---------|------|
| 知识问询 | DC 语义检索 | 知识检索卡 |
| 创建工单 | Create_Ticket | 草稿卡 → 详情卡（回执） |
| 上传附件 | Upload_Ticket_Attachment | 草稿卡附件区（提单时）/ 详情卡追加（提单后） |
| 查工单列表 | List_My_Tickets | 列表卡 |
| 查工单详情 | Get_Ticket_Detail | 详情卡 |
| 确认解决 | Close_Ticket | 详情卡（待确认分支） |
| 驳回 | Reject_Ticket | 详情卡（待确认分支） |
| 评价 | Rate_Ticket | 评价卡 |
| 多次回复 | Reply_Ticket | 详情卡 |

### 7.2 处理人 Skill

| 能力 | MCP Tool | 卡片 |
|------|---------|------|
| 查待办 | List_My_Tickets (agent) | 列表卡 |
| 查详情 | Get_Ticket_Detail | 详情卡 |
| 答复 | Reply_Ticket | 答复卡 |
| 提交已解决 | Resolve_Ticket | 详情卡 |
| 部门内转派 | Reassign_Ticket | 转派卡 |
| 修改分类 | Update_Ticket_Category | 详情卡 |
| 生成知识 | Create_Knowledge_Ingestion | 知识产物卡 |

---

## 八、交互规则与约束

### 8.1 通用规则

1. **fail closed**：所有写操作必须收到后台真实成功回执才显示成功
2. **身份不可篡改**：employeeId 仅来自平台身份
3. **草稿 ≠ 执行**：生成草稿不等于提交
4. **回复 ≠ 状态变更**：回复只发消息，不改状态
5. **数据权限**：Requester 看本人，Agent 看本人分配，Manager 看本组
6. **自然语言 + 卡片双通道**：用户既可以通过卡片按钮操作，也可以直接自然语言指令（如"已解决，我来评价"）。智能体识别意图后先调用对应 Tool，再展示结果对应的卡片状态（如直接展示评分步骤），不需要用户再点按钮重复操作
7. **卡片初始状态可指定**：通过 `data.step` 参数控制卡片初始展示状态（decide/rate/reject），支持智能体根据已完成的 Tool 调用直接呈现对应步骤

### 8.2 附件规则

- 格式：PDF、JPG、PNG、TXT、LOG
- 单文件 ≤ 10MB，最多 5 个
- 全部上传成功才允许建单
- 用勾选框选择包含哪些附件

### 8.3 状态与按钮对应

| 工单状态 | Requester 按钮 | Agent 按钮 | Manager 按钮 |
|---------|--------------|-----------|-------------|
| 新建 | 查看详情 | — | — |
| 待分配 | 查看详情 | — | 分配处理人 / 触发自动分配 |
| 处理中 | 查看详情、多次回复 | 答复、转派、修改分类、提交已解决 | 查看 |
| 待提交人确认 | **确认解决** / **驳回** | 查看 | 查看 |
| 已关闭 | 评价 | 查看、生成知识 | 查看 |

### 8.4 AI 识别

- AI 根据问题描述自动识别：部门、一级分类、优先级
- **智能体提单只处理到一级分类（23 个）**，二级分类（75 个）由 Agent 在处理过程中补充
- 用户不手选优先级，AI 判断后可由 Agent 校正
- AI 输出置信度，低置信度时提示人工确认
- 一级分类清单来自客户提供的 Category Catalog：HR 5 类 + IT 18 类 = 23 类

### 8.5 SLA 规则

- SLA 绑定一级分类（不绑二级）
- 只计有效工作时间，排除周末和节假日
- 首次响应 = 处理人第一次回复
- 解决时间 = 工单进入"待提交人确认"
- 即将超时和已超时发邮件通知

### 8.6 转派规则

| 场景 | 操作人 | 流程 |
|------|--------|------|
| 部门内转派 | Agent | 直接选本组目标人 + 填原因 |
| 跨组转派 | Agent → Manager | Agent 修改分类 → 清除处理人 → 流转本组 Manager → Manager 决定 |
| Manager 直接转交 | Manager | 可指定其他组人员、转交其他分组、或触发自动分配 |

---

## 九、与工单后台系统的关系

| 职责 | 归属 |
|------|------|
| 工单 CRUD、状态流转、分类管理 | 工单后台 |
| 自动分配（按一级分类 + 负载均衡） | 工单后台 |
| SLA 计算（工作时间、节假日排除） | 工单后台 |
| 邮件通知（创建/分配/回复/超时/关闭等） | 工单后台 |
| 看板/仪表盘数据 | 工单后台 |
| 用户/组织/权限/角色管理 | 工单后台 + 灵基身份 |
| AI 识别分类/优先级 | 灵基平台（Skill） |
| 智能体对话、意图识别、知识检索 | 灵基平台（Skill + DC） |
| 业务组件渲染（卡片） | 灵基平台（MCP Apps） |
| Work 菜单页面（我的工单/我的任务/仪表盘） | 工单后台应用嵌入 Work |

**闭环**：工单后台负责状态和业务逻辑，Work 页面负责展示和操作入口，智能体负责对话式交互和卡片呈现。
