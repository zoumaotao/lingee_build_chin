# 灵基 Build 生产化问题清单

> 来源：振兴 HelpDesk POC 项目实际开发过程  
> 目的：推动灵基 Build 标品能力补齐，使其具备生产级开发与交付能力  
> 说明：以下问题均为灵基 Build 作为标准产品、标准开发范式应当具备但当前缺失的能力，非某个项目的定制需求

---

## 一、问题总览

| 维度 | 问题数 | 核心结论 |
|------|--------|----------|
| MCP APPS 能力 | 8 项（#1, #2–#6, #32） | APPS 交互层能力未对外开放、上下文/附件传递不足 |
| Console | 1 项（#1b） | 组件上传入口未对租户开放 |
| DC 知识平台 | 5 项（#7–#9, #8b, #8c） | DC 在写入、智能体沉淀、外部拉取、权限管控方面均有缺口；海外部署已完成 |
| 待讨论 | 2 项（#10, #34） | 部署绑定苍穹；苍穹二开应用无法透出 MCP |
| 消息与事件 | 1 项（#31） | MCP Server 无法主动推送消息给灵基用户/智能体 |
| 国际化 | 4 项（#12–#15） | 海外客户场景下语言支持不达标 |
| 开发范式与工具链 | 15 项（#11, #16–#25, #28–#30, #33） | 端到端流程断裂，APPS 开发割裂，无法连贯开发、测试、上线和多人协作 |
| 模型服务稳定性 | 2 项（#26–#27） | 死循环 / 无限加载 |
| **合计** | **38 项** | **已解决 2 项，待解决 34 项（其中 7 项已有方案设计推进中），待讨论 2 项** |

---

## 二、Console（1 项）

### #1b Console（TManage）自定义组件上传入口未对租户开放

| 项 | 内容 |
|---|------|
| 当前现状 | 自定义 APPS 组件的上传/注册通过 KDManage 的"业务组件"功能完成，但该入口仅内部实施方可操作，租户/客户无法自助上传自己开发的组件包 |
| 标品应有能力 | ① Console（TManage）对租户开放"业务组件"上传入口，客户可自助提交组件包<br>② 支持组件版本管理（上传新版本/回滚旧版本）<br>③ 支持组件审核机制（上传后需审核通过才对智能体可用，或租户自主决定是否需审核）<br>④ 支持组件的启用/停用/下架管理 |
| 为什么是标品问题 | 组件上传能力已存在但未对外开放 = 客户必须依赖金蝶侧代为上传 = 每次组件变更都要走内部流程 = 不可持续。开放后客户才能形成"开发→上传→使用"的自助闭环 |
| 与其他问题的关联 | #1 解决"客户能不能开发"；本条解决"客户能不能自己上传"；#33 解决"能不能在 Build 中开发" |
| 方案推进 | 已纳入 MCP APPS 整体方案，Console 侧 API 需求已列出 → [Build构建MCPAPPS方案设计](重难点问题推进/Build构建MCPAPPS方案设计.md)（第九章 7.2 Console 侧） |
| 状态 | ❌ 未解决（P0）— 方案已设计，待排期开发 |

---

## 三、MCP APPS 能力（8 项）

### #1 APPS 自定义开发能力未对租户开放（规范已有但未公开）

| 项 | 内容 |
|---|------|
| 当前现状 | MCP APPS 的自定义开发规范（dev-guide）、工程模板（demo.zip）、上传机制（KDManage 业务组件）均已存在且内部实施方在使用，但这套能力未对租户/客户开放自助操作。客户无法获取开发规范、无法自行开发和上传自定义组件 |
| 标品应有能力 | 将 APPS 自定义开发能力对租户开放：① 公开开发规范文档和工程模板，让客户/ISV 可基于规范自行开发组件；② 提供版本化的 SDK 和 API 参考（而非内部文档）；③ 明确组件的能力边界、安全约束和审核机制 |
| 为什么是标品问题 | 每个企业的业务交互形态不同，标品不可能穷举所有组件。能力已经有了却不开放 = 客户只能依赖金蝶实施方代做 = 不可规模化、不可持续 |
| 与其他问题的关联 | 规范开放后，还需 Console（TManage）提供客户可自助的上传入口（#1b）；Build 提供开发环境支持（#33） |
| 方案推进 | 已有完整方案设计，涵盖规范公开、SDK 版本化、模板库等 → [Build构建MCPAPPS方案设计](重难点问题推进/Build构建MCPAPPS方案设计.md)（第九章 7.3 APPS 平台侧） |
| 状态 | ❌ 未解决（P0）— 方案已设计，待排期开发 |

---

### #2 APPS 卡片中用户填写的内容不作为 LLM 上下文

| 项 | 内容 |
|---|------|
| 当前现状 | 用户在 MCP APPS 组件（如表单卡片）中填写的内容，不会自动进入 LLM 的会话上下文。模型无法感知用户在卡片中填了什么 |
| 标品应有能力 | 用户在 APPS 组件中填写/选择/提交的内容，应自动纳入当前会话的 LLM 上下文，让模型能基于这些内容进行后续对话（如校验、补全、追问缺失字段等） |
| 为什么是标品问题 | APPS 是智能体与用户的交互层，用户在卡片中的操作本质上就是"用户输入"。如果模型感知不到这些输入，那 APPS 就沦为一个独立的静态表单，无法与对话式智能体联动，失去了 AI-Native 交互的意义 |
| 状态 | ❌ 未解决（P0） |

---

### #2b APPS 与 LLM 的数据传递仅支持全量消息，缺乏精简机制

| 项 | 内容 |
|---|------|
| 当前现状 | 即便 APPS 数据能传递给 LLM，当前仅支持以全量消息的形式塞入对话。例如用户在组件中选择了 100 条数据，必须把 100 条完整内容作为一条对话消息发送给 LLM，用户在界面上也会看到这一大段数据 |
| 标品应有能力 | 支持"对话展示"与"LLM 上下文"分离：① 对话界面向用户仅展示精简提示（如"已选择 100 条数据"）；② 实际数据以用户不感知的形式静默更新到 LLM 的 Session 上下文中。用户看到精简信息，LLM 拿到完整数据 |
| 为什么是标品问题 | 任何涉及列表选择、表单填写、多步操作的场景都会产生结构化数据需传递给 LLM。如果只能全量塞入对话消息，① Token 浪费严重 ② 对话界面不可读 ③ 上下文过长影响模型性能。这是 APPS 与 LLM 协作的基础通信机制问题 |
| 状态 | ❌ 未解决（P0）；对方已确认方案方向 |

---

### #3 APPS 组件渲染性能差，偶发渲染失败

| 项 | 内容 |
|---|------|
| 当前现状 | 会话中的 APPS 组件在滚动离开可视区域后被释放，重新进入时需要重新渲染，渲染时间过长且偶发白屏 |
| 标品应有能力 | APPS 组件应具备合理的缓存与状态恢复机制，确保用户回看历史对话时组件能快速稳定渲染，不丢失交互状态 |
| 为什么是标品问题 | 对话式交互中用户回看上文是高频操作。若每次回看都要等待重新渲染甚至白屏，交互体验无法达到生产级标准。这是 APPS 渲染引擎本身的性能问题 |
| 状态 | ❌ 未解决（P1） |

---

### #4 APPS 组件提交后用户手填内容无法回显

| 项 | 内容 |
|---|------|
| 当前现状 | 用户在 APPS 组件中手动填写/编辑的内容（如修改工单描述、追加备注），提交后回看对话时丢失显示为空白。而模型自动写入的内容（如自动填充的标题、分类、员工信息）则能正常回显。说明组件提交时未将用户的前端编辑状态持久化到服务端消息记录中 |
| 标品应有能力 | 组件提交时应将完整的表单数据（含用户手填部分）持久化到服务端消息记录。回看时从持久化数据渲染，而非依赖组件内存状态 |
| 为什么是标品问题 | 用户手填内容往往是最关键的信息（如工单的问题描述正文）。提交后回看丢失 = 用户无法确认自己填了什么 = 对话作为操作记录的可靠性被破坏 |
| 状态 | ❌ 未解决（P1） |

---

### #5 APPS 支持获取灵基登录用户信息

| 项 | 内容 |
|---|------|
| 当前现状 | MCP APPS 此前无法获取当前灵基登录用户的身份信息 |
| 标品应有能力 | APPS 组件应能获取当前会话用户的基本身份信息（用户 ID、姓名、邮箱、部门等），用于个性化交互和数据隔离 |
| 为什么是标品问题 | 几乎所有企业级交互场景都需要基于用户身份做数据隔离和个性化展示。这是 APPS 与灵基身份体系的基础集成能力 |
| 状态 | ✅ 已解决（7.20 完成，部署在 83 环境；生产环境尚未上线） |

---

### #6 Agent 无法感知 MCP APP 组件的内部业务逻辑

| 项 | 内容 |
|---|------|
| 当前现状 | Agent 对 MCP APP 组件的了解仅限于入参、出参和名称/描述文本，无法知道组件内部有哪些交互元素和具体业务逻辑 |
| 标品应有能力 | 应提供结构化的组件能力描述机制（如 JSON Schema 描述组件内部事件、操作、状态变化），让 Agent 能准确理解组件的交互能力边界 |
| 为什么是标品问题 | Agent 编排 Skill 时需要理解"调用这个 APP 能做什么"。只能靠文字描述猜测 = 编排精度受限 = AI 辅助开发的上限被锁死。这是 APPS 平台的元信息表达能力不足 |
| 状态 | ❌ 未解决（P3） |

---

### #32 APPS 无法访问和操作对话中的附件（图片/文件）

| 项 | 内容 |
|---|------|
| 当前现状 | 用户在对话中发送图片（如错误截图）或文件（如 PDF）后触发提单，APPS 工单确认卡渲染时：① 无法从 Session 上下文中获取对话里已有的附件列表；② 无法在卡片内展示"已关联附件"供用户确认/增删；③ 用户确认提交后，附件引用无法随表单数据一起传递给 MCP Tool（如 Upload_Attachment） |
| 标品应有能力 | ① APPS 组件渲染时可从当前 Session 上下文中读取对话附件列表（文件名/类型/大小/引用 ID）<br>② 组件中可展示附件缩略图/列表，支持用户勾选、删除或追加新附件<br>③ 组件提交时将附件引用作为结构化字段传递给后续 MCP Tool 调用<br>④ 若平台 LLM 支持多模态（视觉理解），可基于图片内容辅助生成工单描述 |
| 为什么是标品问题 | "截图+描述问题→提单"是企业服务最常见的交互模式。APPS 无法操作对话附件 = 智能体只能处理纯文本 = 与传统邮件工单无差异。这是 APPS 作为 AI-Native 交互层的基础能力缺失 |
| 振兴实际场景 | PRD 明确要求：① 工单草稿确认卡和补充信息卡都要提供**上传附件入口**，用户可在对话中已有附件之外追加新附件；② 确认卡需展示全部字段**和附件**供用户确认；③ 附件需全部上传成功才能建单，任一失败则保留草稿不建单；④ 客户限定附件格式为 PDF/JPG。实际场景：员工描述"电脑蓝屏"时发一张错误截图+一段文字，触发提单后，工单确认卡应自动带出该截图作为附件，用户可确认/删除/追加后再提交 |
| 状态 | ❌ 未解决（P1） |

---

## 四、DC 知识平台能力（3 项）

### #7 DC 海外部署

| 项 | 内容 |
|---|------|
| 当前现状 | **已完成海外部署（新加坡节点）** |
| 标品应有能力 | DC 应支持多区域部署，至少覆盖东南亚 |
| 为什么是标品问题 | 灵基面向全球客户，DC 不支持海外 = 海外客户知识能力不可用 = 智能体无法运行 |
| 状态 | ✅ 已解决 |

---

### #8 DC 缺少标准的知识写入 API（外部系统推送写入）

| 项 | 内容 |
|---|------|
| 当前现状 | DC 暂不支持外部系统（如工单后台/MCP Server）通过 API 向指定智能体知识库写入知识。缺乏写入时的关联标识（集群、租户、智能体、实例），没有增量更新和版本管理机制 |
| 标品应有能力 | DC 应提供标准的知识写入 API，允许经过鉴权的外部系统向指定智能体知识库推送结构化知识。需支持：鉴权、关联到指定智能体/实例、增量更新、写入去重、版本管理<br>**（实现上可与 #8b 合并为统一写入 API，通过调用方身份区分来源）** |
| 为什么是标品问题 | 典型场景：工单处理结论、客服对话总结、业务规则变更等需要从后台系统实时写入 DC。没有写入 API = 知识只能人工上传 = 无法形成知识飞轮 |
| 状态 | ❌ 未解决（P1）；DC 团队 8 月底初版 |

---

### #8b DC 缺少灵基智能体运行时自动沉淀知识的能力

| 项 | 内容 |
|---|------|
| 当前现状 | 灵基智能体在运行过程中（如对话中识别到高频问题、处理人总结 FAQ），无法将生成的知识自动写入所属智能体的知识库。智能体只能"消费"知识，不能"生产"知识 |
| 标品应有能力 | 灵基智能体运行时应能通过内部接口将结构化知识写入自身（或指定）智能体的 DC 知识库，支持自动归档、标记来源（来自哪次对话/哪个工单）、质量审核机制（自动写入 vs 人工审核后写入）<br>**（实现上可与 #8 合并为统一写入 API，智能体作为内部调用方走相同接口）** |
| 为什么是标品问题 | 智能体的核心价值之一是"越用越聪明"。如果智能体只能被动消费初始知识而不能在运行中自动沉淀新知识，就无法实现自主进化。这是"知识飞轮"闭环的最后一环 |
| 状态 | ❌ 未解决（P1） |

---

### #8c DC 缺少主动拉取外部数据源的连接器框架

| 项 | 内容 |
|---|------|
| 当前现状 | DC 无法主动从外部数据源（如 SharePoint、Confluence、Google Drive 等）拉取文档并增量同步到知识库。当前唯一的方式是人工下载后手动上传 |
| 标品应有能力 | DC 应具备通用的外部数据源连接器框架：① 支持对接主流文档平台（SharePoint/Confluence/Google Drive 等）；② 支持 OAuth 等标准认证协议；③ 支持增量变更检测（仅同步新增/修改的文件）；④ 支持定时调度或 Webhook 触发同步；⑤ 同步后自动进入 DC 标准加工流程（解析→向量化→索引） |
| 为什么是标品问题 | 企业知识大量存在于 SharePoint 等现有平台中，且持续更新。DC 不能主动拉取 = 知识只能一次性人工搬运 = 知识时效性无法保障。振兴 2TB SharePoint 内容是 Phase 3 核心需求，但 DC 连接器是通用平台能力而非项目定制 |
| 状态 | ❌ 未解决（P2，Phase 3 前需就绪） |

---

### #9 DC 缺少完整的知识权限体系（检索越权仅是表面现象）

| 项 | 内容 |
|---|------|
| 当前现状 | 用户可检索到不属于自己权限范围的知识内容。深层原因是 DC 在知识权限的五个层面均未建设：<br>① **权限模型缺失**——知识按什么维度隔离（智能体实例/租户/部门/文档级）未定义<br>② **权限标记机制缺失**——知识入库时无法打权限标签（人工标记/继承外部 ACL 均不支持）<br>③ **检索时权限过滤缺失**——向量检索不做权限拦截，全量返回后无 post-filter<br>④ **与灵基用户体系未打通**——DC 不知道当前检索用户的角色/部门/所属实例，无法做动态权限判断<br>⑤ **权限管理界面缺失**——运营者无处配置"谁能看什么知识"的规则 |
| 标品应有能力 | ① 定义知识权限模型：支持按智能体实例隔离（基础）+ 按部门/角色/用户组细粒度标记（进阶）<br>② 知识入库时支持打权限标签：手动标记 / 基于上传来源自动继承 / 映射外部文档权限（如 SharePoint ACL）<br>③ 检索引擎支持 pre-filter 或 post-filter 的权限过滤，确保用户只能获取有权内容<br>④ DC 检索接口自动接收灵基用户身份信息（角色/部门/实例），无需调用方额外传递<br>⑤ 提供权限配置管理界面，支持批量规则（如"IT 部门可见 IT 类知识"）、实时生效、变更审计 |
| 为什么是标品问题 | 知识权限管控是企业级产品的基本安全要求。振兴有 3 条业务线（Tech GBS / People GBS / BP），知识必须隔离；未来对接 SharePoint 后权限继承更复杂。这不是修一个检索 Bug 就能解决的——是一整套权限体系需要从零建设 |
| 状态 | ❌ 未解决（P1） |

---

## 五、待讨论（2 项）

### #10 Build 开发的应用仅支持部署到苍穹

| 项 | 内容 |
|---|------|
| 当前现状 | Build 开发的后端应用只能托管到苍穹平台，而苍穹有独立的许可体系和账号体系。灵基用户（多数非 ERP 用户）没有苍穹账号，无法访问部署在苍穹上的应用 |
| 标品应有能力 | Build 应支持将应用部署到多种目标环境：苍穹（现有）/ 灵基自身轻量级应用托管 / 客户自有云服务器。应用身份验证应直接基于灵基用户体系，不强制依赖苍穹身份 |
| 为什么是标品问题 | 灵基的目标用户群远大于 ERP 用户群。Build 应用只能跑在苍穹上 = 使用者必须有苍穹身份 = 灵基适用场景被限制在 ERP 用户圈内 |
| 待讨论 | 需先确认：灵基应用托管能力是否已在规划中？苍穹绑定是否有架构层面的必要性？确认后再定优先级 |
| 状态 | ⏸️ 待讨论 |

---

### #34 苍穹不支持客户二开应用透出 MCP Server 供灵基调用

| 项 | 内容 |
|---|------|
| 当前现状 | 客户在苍穹上有二次开发的业务应用（自定义接口），但苍穹不支持将这些接口暴露为 MCP Server。灵基智能体只能连接灵基标品预置的 MCP Server，无法调用客户在苍穹上已有的业务能力 |
| 标品应有能力 | 苍穹应支持客户将二开应用的接口注册/透出为标准 MCP Server，使灵基智能体可以直接连接调用。或者灵基侧提供标准适配器/网关，能对接苍穹的非 MCP 接口并转换为 MCP 协议 |
| 为什么是标品问题 | 大量企业客户在苍穹上已有定制化业务应用（ERP 扩展、审批流程、业务查询等）。如果这些已有能力无法被灵基智能体调用，客户要么重新开发一套 MCP Server（重复建设），要么放弃让智能体连接已有业务——两者都不可接受 |
| 待讨论 | 推动方向需确认：① 苍穹侧提供"自定义 MCP 透出"能力？② 灵基侧提供"苍穹接口→MCP 协议"的适配网关？③ 两者结合？ |
| 状态 | ⏸️ 待讨论 |

---

## 六、国际化与多语言（4 项）

### #12 灵基客户端多语言翻译错误

| 项 | 内容 |
|---|------|
| 当前现状 | 灵基客户端界面存在多处翻译错误或未国际化文案 |
| 标品应有能力 | 面向海外市场的客户端应完成全量文案国际化，翻译质量经 native speaker 审核 |
| 为什么是标品问题 | 多语言支持是产品面向全球市场的基础能力，翻译错误直接影响产品专业度和客户信心 |
| 状态 | ❌ 未解决（P1） |

---

### #13 Build 板块全英配置下 AI 回复偶现中文

| 项 | 内容 |
|---|------|
| 当前现状 | 系统已配置 AI Response Language = English，但 AI 实际输出仍然中英文夹杂 |
| 标品应有能力 | AI 响应语言应严格遵循系统配置。配置为英文时 AI 全部输出均应为英文 |
| 为什么是标品问题 | 语言设置是平台级配置项，不生效说明 Prompt 层语言控制逻辑存在缺陷 |
| 状态 | ❌ 未解决（P1） |

---

### #14 Work 板块全英配置下 AI 回复偶现中文

| 项 | 内容 |
|---|------|
| 当前现状 | Work 板块已配置全英文环境，AI 回复偶尔仍使用中文 |
| 标品应有能力 | 同 #13，Prompt 级语言控制应稳定 |
| 为什么是标品问题 | 同 #13 根因 |
| 状态 | ❌ 未解决（P1） |

---

### #15 Web/客户端语言切换需反复操作三遍

| 项 | 内容 |
|---|------|
| 当前现状 | 切换界面语言需反复操作三遍才能生效 |
| 标品应有能力 | 语言切换应一次操作即时生效 |
| 为什么是标品问题 | 基础设置操作的 Bug |
| 状态 | ❌ 未解决（P1） |

---

## 七、开发范式与工具链（15 项）

### #16 测试智能体必须先将其依赖的技能上线

| 项 | 内容 |
|---|------|
| 当前现状 | 开发阶段若要测试一个智能体，必须先将其依赖的技能走完"提交→审核→上线"的完整流程。技能未上线 = 智能体无法调试 |
| 标品应有能力 | 开发态下智能体应能直接引用未上线的技能进行联调测试。"上线"是发布行为，不应成为"调试"的前置条件 |
| 为什么是标品问题 | "先上线才能测试"违反软件工程基本流程（开发→测试→上线），这是 Build 开发范式本身的逻辑错误，任何使用 Build 的开发者都会遇到 |
| 方案推进 | "开发测试一体化"方案直接回应此痛点——目标是让用户在 Build 内直接测试，不再依赖先上线 → [开发测试一体化方案设计](重难点问题推进/开发测试一体化/开发测试一体化方案设计.md) |
| 状态 | ❌ 未解决（P2）— 方案对比中，待决策 |

---

### #17 同名 Skill 点击测试会静默覆盖线上已运行版本

| 项 | 内容 |
|---|------|
| 当前现状 | 开发者创建与线上 Skill 同名的新版本时，点击"测试"会直接覆盖线上正在运行的 Skill，无任何提醒或确认 |
| 标品应有能力 | ① 测试阶段：同名 Skill 的测试应在沙箱进行，不影响线上；② 上线阶段：检测到同名时明确提醒，让用户选择"更新现有版本"或"放弃" |
| 为什么是标品问题 | 同名 Skill 在迭代更新中非常常见。平台缺乏冲突处理机制，静默覆盖线上版本可能导致生产环境故障。这是基本的安全防护缺失 |
| 状态 | ❌ 未解决（P1） |

---

### #18 智能体的 MCP Server 关联与知识挂载无法在 Build 中连贯完成

| 项 | 内容 |
|---|------|
| 当前现状 | ① 智能体要连接的 MCP Server 需在 Console 中创建实例时关联，Build 中无法完成；② 知识也需在 Console 创建实例时关联；③ 知识同时又开放在 Work-智能体-知识中上传。开发者需在 Build → Console → Work 三处反复切换 |
| 标品应有能力 | ① 开发者应能在 Build 中一站式完成智能体全部配置（含 MCP Server 关联、知识挂载）；② 明确区分"开发者视角"和"运营者视角"的配置入口 |
| 为什么是标品问题 | 智能体 = 提示词 + 技能 + 知识 + 外部连接。四要素配置分散在三个模块，任何开发者构建智能体时都会面临同样的割裂体验 |
| 状态 | ❌ 未解决（P3） |

---

### #19 技能/智能体没有版本管理

| 项 | 内容 |
|---|------|
| 当前现状 | 技能/智能体缺乏版本管理机制，无法查看历史版本、版本对比、版本回滚 |
| 标品应有能力 | 每次提交/上线形成版本快照，支持版本列表查看、Diff 对比、一键回滚 |
| 为什么是标品问题 | 版本管理是所有软件制品的基础管理能力。没有版本管理 = 无法追溯变更、无法安全回滚、无法多人协作。这是企业级开发平台的基本门槛 |
| 方案推进 | 已有完整顶层设计（两层版本模型 + 分阶段实施），Phase 1a 极简版预计 2-3 天可交付 → [Build统一版本管理能力方案设计](重难点问题推进/Build统一版本管理能力方案设计.md) |
| 状态 | ❌ 未解决（P2）— 方案已设计，Phase 1a 可快速交付 |

---

### #20 智能体实例创建流程存在对苍穹的不明确依赖

| 项 | 内容 |
|---|------|
| 当前现状 | 创建智能体实例时需在苍穹中新增一个 Bot 机器人，为什么需要这步、具体需要什么权限均不明确 |
| 标品应有能力 | ① 首先澄清：创建实例是否真的需要依赖苍穹？不需要则解除耦合；② 如确需，应在灵基侧自动化完成，不要求开发者手动操作苍穹 |
| 为什么是标品问题 | 与 #10 属同一架构耦合问题。没有苍穹环境的团队将无法完成智能体部署 |
| 状态 | ❌ 未解决（P3，需先澄清） |

---

### #21 智能体创建 Skill 后缺乏同步批量发布机制

| 项 | 内容 |
|---|------|
| 当前现状 | AI 辅助创建智能体时可能自动创建配套 Skill，但智能体发布依赖 Skill 先发布，需手动逐个按顺序发布 |
| 标品应有能力 | 一键发布智能体时，自动检测并同步发布其依赖的未上线 Skill（或至少提示+批量操作） |
| 为什么是标品问题 | 智能体与 Skill 存在天然依赖关系，发布流程应感知这层依赖，而非让开发者手动管理发布顺序 |
| 状态 | ❌ 未解决（P3） |

---

### #22 Build 中更换 Agent 的 Skill 后工作区未同步

| 项 | 内容 |
|---|------|
| 当前现状 | 通过对话指令更换 Agent 绑定的 Skill 后，对话显示已成功，但工作区文件和配置没有同步更新 |
| 标品应有能力 | 对话式操作的结果应实时同步到工作区文件和配置中，保持所见即所得 |
| 为什么是标品问题 | Build 的核心模式是"对话驱动开发"。对话中的操作不真实反映到工作区 = 开发者无法信任对话结果 = 对话式开发的可靠性被根本破坏 |
| 状态 | ❌ 未解决（P3） |

---

### #23 技能在智能体中被修改后无法同步上线

| 项 | 内容 |
|---|------|
| 当前现状 | 在智能体对话中修改了技能内容后：① 提交智能体时只能发布智能体本身，技能发不上去；② 需手动将修改复制回技能源文件；③ 再单独测试/提交技能。极易遗漏 |
| 标品应有能力 | ① 在智能体上下文中修改技能后，修改自动同步回技能源文件；② 或提交智能体时检测技能有变更，提示一起提交 |
| 为什么是标品问题 | 在智能体中迭代修改技能是高频操作。"手动复制回源文件"极易遗漏，导致线上运行旧版本技能而开发者不自知——这是隐性线上 Bug 的主要来源 |
| 状态 | ❌ 未解决（P2） |

---

### #24 文件修改后文件树无状态标记

| 项 | 内容 |
|---|------|
| 当前现状 | 文件被修改后，左侧文件树中没有任何直观的状态标记 |
| 标品应有能力 | 文件树应显示文件状态（已修改/未提交/待审核等），类似 Git 状态标记 |
| 为什么是标品问题 | 多文件项目中需快速识别变更范围。无状态标记 = 只能靠记忆管理文件状态 = 复杂项目中极易遗漏提交 |
| 状态 | ❌ 未解决（P3） |

---

### #25 提交同名技能因名称重复失败，不支持更新语义

| 项 | 内容 |
|---|------|
| 当前现状 | 将修改后的技能重新提交时，因名称重复而失败。系统将"修改后重新提交"等同于"新建同名"而拒绝 |
| 标品应有能力 | ① 修改后重新提交应识别为"版本更新"而非"新建"；② 引入除名称外的唯一标识（ID）区分新建与更新 |
| 为什么是标品问题 | 技能迭代更新是日常操作。"更新已有技能"这个最基本的操作走不通 = 开发者被迫改名或先删旧版本 |
| 状态 | ❌ 未解决（P3） |

---

### #26 Build 使用 fast 模型时进入死循环

| 项 | 内容 |
|---|------|
| 当前现状 | 使用 fast 模型进行对话开发时，模型会不定期进入死循环（反复输出相同内容或循环调用） |
| 标品应有能力 | 模型服务应具备循环检测和自动中断机制 |
| 为什么是标品问题 | 模型服务是 Build 的底层基础设施，基础设施不稳定 = 所有上层开发活动受影响 |
| 状态 | ❌ 未解决（P2） |

---

### #27 Build 使用 deep 模型时回复无限加载

| 项 | 内容 |
|---|------|
| 当前现状 | 使用 deep 模型时，AI 回复有时一直处于加载状态（无限转圈），无法正常输出，只能强制终止 |
| 标品应有能力 | 应有超时检测和优雅降级机制（超时后提示重试或自动降级到其他模型） |
| 为什么是标品问题 | 同 #26，模型服务稳定性是平台基础能力 |
| 状态 | ❌ 未解决（P2） |

---

### #28 端到端开发流程完全断裂（最关键的系统性问题）

| 项 | 内容 |
|---|------|
| 当前现状 | 一个完整功能的开发需要在 5+ 个工具/环境中反复切换，各环节之间没有任何自动化衔接 |

**当前实际开发流程：**

```
1. 产品设计（Kiro/其他 AI 工具）：需求拆解 → Agent 设计 → Skill 设计 → 交互需求 → 应用设计
   ↓ 人工交接（文档搬运）
2. MCP APPS 开发（本地 IDE）：开发调试 → 上传到 KDM
   ↓ 手动配置
3. Skill 开发（生产灵基 Build）：对话开发 → 提交到沙箱 Console → 在沙箱 Work 调试
   ↓ 手动切换环境
4. 智能体开发（生产灵基 Build）：对话开发 → 提交到沙箱 Console → 在沙箱 Work 调试
   ↓ 手动切换环境
5. 苍穹应用开发（本地苍穹 + Build）：本地苍穹 → Build 连接 → 对话开发 → 传统苍穹上线流程 → 配 MCP Server 地址
   ↓ 全部就绪后
6. 端到端联调（沙箱 Work）：此时才能跑通完整链路
```

| 项 | 内容 |
|---|------|
| 标品应有能力 | **终态：** 灵基 Build 提供一站式项目工作空间，从需求→设计→开发→测试→上线在同一上下文完成<br>**初阶段最低要求：** ① 需求设计产物能自动传递到开发环节作为输入；② APPS/Skill/智能体/应用的开发成果能在同一沙箱联调，无需手动逐个配置；③ 提交/发布有依赖感知和批量操作能力；④ 环境切换有明确引导，状态可追溯 |
| 为什么是标品问题 | 这不是某个项目流程设计的问题，而是 Build 作为开发平台本身缺乏工程化管理能力的系统性表现。任何开发超过"单技能/单智能体"复杂度的项目都会面临完全相同的断裂体验。这是 Build 从"演示工具"走向"生产级开发平台"必须解决的根本问题 |
| 方案推进 | "开发测试一体化"已有两个方案对比（方案一按数据分态 vs 方案二按环境隔离），待决策拍板 → [开发测试一体化方案设计](重难点问题推进/开发测试一体化/开发测试一体化方案设计.md) |
| 状态 | ❌ 未解决（P2）— 方案对比中，待决策 |

---

### #29 已发布技能更新内容后无法触发状态变更

| 项 | 内容 |
|---|------|
| 当前现状 | 已发布的技能，无论是直接更新本地文件内容，还是在对话框中让 AI 修改描述，都无法触发状态从"已发布"变为"已保存"，导致无法重新提交。唯一 workaround 是让 AI 做无关改动（如改描述语言）才能触发状态变化 |
| 标品应有能力 | 技能文件内容变更后（无论通过什么方式），系统应自动检测到变更并将状态标记为"已保存/待提交" |
| 为什么是标品问题 | 技能迭代更新是最基本的日常操作。"改了内容却提交不了"属于状态检测机制的基础 Bug |
| 状态 | ❌ 未解决（P2） |

---

### #30 Build 不支持多人协同开发技能和智能体

| 项 | 内容 |
|---|------|
| 当前现状 | Build 的开发模式是单人单会话，当多人协作开发同一个智能体时没有任何协同机制：① 没有共享项目/工作空间——每人只能在自己的会话中工作；② 没有技能交付标准流程——只能通过聊天传文件；③ 接收方无标准导入方式——只能"删除本地文件夹再上传"，且会触发 #29 的状态 Bug；④ 没有变更合并机制；⑤ 没有协作可见性 |
| 标品应有能力 | ① 共享项目空间：多人加入同一项目，共享同一套制品；② 标准技能交付流程：支持"发布到项目"，接收方一键拉取最新版；③ 变更感知：协作者更新后其他人能看到变更提示；④ 至少支持"分工→集成"模式：不同人负责不同技能，集成到同一智能体时有工具支持而非手动文件搬运 |
| 为什么是标品问题 | 任何超过"一人做一个简单智能体"的项目都需要多人协作。Build 无协同机制 = 只能用于个人实验，无法支撑团队级生产项目。这是从"个人工具"走向"团队开发平台"的关键缺失 |
| 状态 | ❌ 未解决（P2） |

---

### #33 MCP APPS 开发完全游离于 Build 之外，无法在 Build 中构建和调试

| 项 | 内容 |
|---|------|
| 当前现状 | MCP APPS 组件的开发必须在本地 IDE 中进行（编码→编译→打包），完成后手动上传到 KDM 平台注册。整个过程与 Build 完全割裂：Build 既不能创建 APPS 项目，也不能调试 APPS 组件的渲染效果，更无法在 Build 中将 APPS 与 Skill/智能体进行联调 |
| 标品应有能力 | ① Build 应支持 APPS 组件的创建和开发（或至少提供脚手架生成 + 本地开发引导）<br>② Build 中应能预览和调试 APPS 组件的渲染效果<br>③ APPS 组件发布后应能在 Build 中直接关联到 Skill/智能体，无需跳转 KDM 手动配置<br>④ 至少：Build 与本地 APPS 开发之间有自动化衔接（如 CLI 工具、热更新调试通道） |
| 为什么是标品问题 | APPS 是智能体与用户交互的核心层，但其开发与 Build 完全割裂 = Build 号称的"一站式开发"天然缺了交互层。开发者被迫在 Build（Skill/智能体）和本地 IDE（APPS）之间反复切换，这是 #28 端到端断裂在 APPS 维度的具体表现 |
| 与其他问题的关联 | #1 解决"规范是否对外开放"；#1b 解决"客户能否自助上传"；本条解决"能否在 Build 中完成 APPS 开发和联调"。三者需拉通实现 |
| 方案推进 | 已有完整方案设计，涵盖对话式创建/预览/构建打包/一键提交全流程，含任务拆解 → [Build构建MCPAPPS方案设计](重难点问题推进/Build构建MCPAPPS方案设计.md) |
| 状态 | ❌ 未解决（P2）— 方案已设计，待各方评审 |

---

### #11 应用开发要求过多不相关文件权限

| 项 | 内容 |
|---|------|
| 当前现状 | 在 Build 中进行应用开发时，系统要求授予大量与当前开发任务不相关的文件权限 |
| 标品应有能力 | 应遵循最小权限原则，仅请求当前任务必需的资源权限，权限用途应有清晰说明 |
| 为什么是标品问题 | 过度权限请求违反基本安全原则，在企业客户环境中会触发安全审批流程、拖慢交付 |
| 状态 | ❌ 未解决（P3） |

---

## 八、消息与事件能力（1 项）

### #31 灵基缺少接收 MCP Server 主动推送自定义格式消息的平台能力

| 项 | 内容 |
|---|------|
| 当前现状 | 灵基智能体连接 MCP Server 后，目前主要支持"灵基发起请求、MCP Server 返回结果"的同步调用模式。MCP Server 无法在工单分配、处理人答复、SLA 告警等后台事件发生时，主动向指定灵基用户、工作组或智能体实例推送自定义格式消息。后台事件无法可靠进入灵基，用户和处理人就不能真正把灵基作为唯一工作台 |
| 标品应有能力 | ① 提供标准的消息/事件接入协议，使已鉴权的 MCP Server 或后台应用可主动投递事件<br>② 支持按用户、工作组、角色、智能体及智能体实例路由<br>③ 支持文本、通知、卡片、按钮等自定义消息 Schema，并由 Work 正确渲染和交互<br>④ 支持消息队列、幂等、重试、失败告警、离线补发、已读/未读和回执<br>⑤ 支持后台应用→智能体、智能体→智能体等跨主体消息传递，并具备权限校验和审计<br>⑥ 消息到达目标智能体后，应支持触发智能体自动执行预设动作（事件驱动），而非仅被动等待用户打开对话才展示 |
| 为什么是标品问题 | 异步事件推送不是 HelpDesk 私有需求。审批待办、告警、任务分配、业务状态变化等企业场景都要求后台主动触达用户。只有请求—响应能力的 MCP 连接只能支撑"用户问、系统答"，不能支撑持续运行的业务工作台。这是灵基平台通用的消息与事件基础设施缺口 |
| 与项目需求的边界 | 灵基标品负责通用的接入、路由、格式、可靠投递与安全审计；HelpDesk 项目仅负责定义"新工单、答复、超时、邀评"等具体业务事件和卡片内容 |
| 方案推进 | 已有详细三阶段方案（方案A事件触发代发 / 方案B原生消息 / 方案C待办页面兜底）。Harness 侧事件触发基础能力预计 821 上线 → [灵基消息与事件推送能力方案设计](重难点问题推进/灵基消息与事件推送能力方案设计.md) |
| 状态 | ❌ 未解决（P0）— 方案已设计，Harness 821 基础能力即将上线 |

---

## 九、优先级建议

### P0 — 阻塞生产上线（4 项）

| # | 问题 | 理由 |
|---|------|------|
| #1 | APPS 自定义开发规范未对租户开放 | 能力已有但不开放 = 客户只能依赖实施方代做 |
| #1b | Console 组件上传入口未对租户开放 | 客户无法自助上传 = 每次变更都要走内部流程 |
| #2 | APPS 卡片填写内容不进入 LLM 上下文 | 模型无法感知用户在卡片中的输入 |
| #2b | APPS 与 LLM 数据传递缺乏精简机制 | Token 浪费、界面污染、上下文过长 |
| #31 | MCP Server 无法主动推送消息 + 缺少事件驱动 | 唯一工作台闭环无法成立 |

### P1 — 严重影响交付质量（8 项）

| # | 问题 | 理由 |
|---|------|------|
| #8 | DC 知识写入 API（外部推送） | 知识飞轮基础 |
| #8b | 智能体运行时自动沉淀 | 知识飞轮闭环最后一环 |
| #9 | 知识检索越权 | 数据安全是企业客户底线 |
| #3 | APPS 渲染性能 | 组件白屏直接破坏用户操作 |
| #4 | APPS 内容回显 | 操作不可追溯影响信任 |
| #12–#15 | 多语言问题（4 项） | 海外客户无法接受 |
| #17 | 同名 Skill 覆盖线上 | 生产安全红线 |
| #32 | APPS 无法操作对话附件 | "截图+描述"提单走不通 |

### P2 — 严重影响开发效率（7 项）

| # | 问题 | 理由 |
|---|------|------|
| #28 | 端到端流程断裂 | 根因问题，决定 Build 能否做复杂项目 |
| #16 | 测试需先上线技能 | 违反开发基本流程 |
| #19 | 无版本管理 | 企业级基本门槛 |
| #23 | 技能修改不同步 | 隐性线上 Bug 来源 |
| #29 | 状态检测 Bug | 改了提交不了 |
| #30 | 无多人协同 | 团队项目走不通 |
| #33 | APPS 开发游离于 Build 之外 | Build"一站式"天然缺交互层 |
| #8c | DC 缺少外部数据源连接器 | SharePoint 同步走不通 |
| #26/#27 | 模型稳定性 | 打断开发流程 |

### P3 — 体验与完善（8 项）

| # | 问题 | 理由 |
|---|------|------|
| #6 | Agent 感知 APP 逻辑 | AI 开发能力上限 |
| #11 | 权限过多 | 安全合规 |
| #18 | MCP/知识关联流程 | 配置体验优化 |
| #20 | 创建实例依赖苍穹 | 需先澄清合理性 |
| #21 | 缺乏批量发布 | 流程便捷性 |
| #22 | 工作区同步 | 可靠性修复 |
| #24 | 文件状态标记 | 开发体验 |
| #25 | 同名提交失败 | 更新机制修复 |

---

## 十、总结

灵基 Build 当前处于**从演示工具向生产级开发平台过渡**的关键阶段。上述 38 项问题（已解决 2 项，待解决 34 项，待讨论 2 项）不是某个项目的特殊需求，而是任何尝试用 Build 做生产级交付的团队都会遇到的标品能力缺口。

核心矛盾：

1. **APPS 能力不足** — 智能体与用户的交互层撑不住生产级场景
2. **DC 能力未完全就绪** — 知识在安全性、写入和持续治理方面仍有缺口
3. **消息与事件能力缺失** — 后台无法主动触达灵基用户和智能体，灵基无法成为持续运行的统一工作台
4. **开发范式未成型** — 从设计到上线及多人协作的全流程断裂，Build 还不是一个可以做团队工程的工具

**解决路径：** P0 必须在第一个客户上线节点前完成；P1 在首批用户推广前完成；P2 作为 Build 产品的持续迭代目标。
